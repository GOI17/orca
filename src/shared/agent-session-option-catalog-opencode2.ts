import type { AgentSessionOptionCatalog } from './agent-session-option-catalog-types'

/** OpenCode reports its account- and project-specific model catalog at runtime. */
export const OPENCODE2_SESSION_OPTION_CATALOG: AgentSessionOptionCatalog = {
  models: [],
  modelApply: {}
}
